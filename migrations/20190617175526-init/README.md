# Migration `20190617175526-init`

This migration has been generated by Harshit Pant at 6/17/2019, 5:55:26 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."User" ("id" INTEGER DEFAULT 0 NOT NULL, "username" TEXT DEFAULT '' NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Post" ("id" INTEGER DEFAULT 0 NOT NULL, "data" TEXT DEFAULT '' NOT NULL, "user" INTEGER REFERENCES User(id), PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190617175526-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,21 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id       Int    @id
+  username String @unique
+  posts    Post[]
+}
+
+model Post {
+  id   Int    @id
+  data String
+  user User?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190617175526-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190617175526-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
